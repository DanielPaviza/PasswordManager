<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PasswordManager.ViewModels"
             x:DataType="vm:LoginViewModel"
             x:Class="PasswordManager.Views.LoginView">
	<StackPanel Margin="40" Spacing="10">
		<TextBlock Text="Enter Master Password" FontSize="18" />
		<TextBox PasswordChar="•" Text="{Binding InputPassword, Mode=TwoWay}" />
		<TextBlock Text="{Binding ValidationMessage}" Foreground="Red" FontSize="12"/>
		<Button Content="Login" Command="{Binding LoginCommand}" />
	</StackPanel>
</UserControl>
